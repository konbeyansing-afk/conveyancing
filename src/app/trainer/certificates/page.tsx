/**
 * The trainer's certificate queue is the same page as the admin's — it already
 * scopes what it shows to the trainees the viewer supervises, so there is one
 * implementation rather than two that can drift apart.
 */
export { default } from "@/app/admin/certificates/page";
